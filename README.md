# Register File Parser

## Build project
Run this commands in the */build* directory 
```
cmake -G "MinGW Makefiles" ..
cmake --build .
```
## Run project
Run the executable file *RegisterFileParser.exe* in the */build* directory with the following command:
```./RegisterFileParser.exe -f <input_file> -o <output_file>```
